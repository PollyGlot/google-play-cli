# API Coverage Matrix

<!-- Generated by `make coverage-update`. Do not edit by hand: an offline test
     compares this file against a re-render and fails on any hand-edit. -->

Every method of every API gplay snapshots, in one of five states. Generated
from `docs/discovery/paths.txt` (the method universe) and `internal/apiregistry`
(the methods shipped commands call, plus its exclusion, redundancy and parking
lists), so a row cannot claim coverage the code does not have.

Policy: under [ADR-0026](adr/0026-maximal-admin-api-coverage.md) **every Play
admin API is in scope**; only *runtime* APIs (purchase-token verification, Play
Integrity) are excluded by nature. [ADR-0047](adr/0047-coverage-dispositions-redundant-and-parked.md)
adds two declared dispositions: a method is *redundant* when a called method
already serves the same resource in another shape (the canonical is named on
the row, and an offline test fails if it ever stops being called), and *parked*
when its decision is deferred to a `type:parking` issue (linked on the row).
Anything else uncovered is a real gap, not a judgement about whether it is
worth shipping.

| Mark | State | Meaning |
|---|---|---|
| ✅ | called | at least one shipped `gplay` command calls it |
| ⚪ | redundant | same resource, another shape; the canonical named on the row is called |
| ⚫️ | excluded | never to be wrapped, with the reason on the row |
| 🔴 | parked | deferred, the row links its `type:parking` issue |
| 🔴 | uncovered | in scope, no command calls it, no disposition yet |

## Headline

| Service | Methods | ✅ called | ⚪ redundant | ⚫️ excluded | 🔴 parked | 🔴 uncovered |
|---|---:|---:|---:|---:|---:|---:|
| `androidpublisher` v3 | 145 | 93 | 31 | 11 | 10 | 0 |
| `playdeveloperreporting` v1beta1 | 25 | 24 | 0 | 0 | 1 | 0 |
| `gamesConfiguration` v1configuration | 10 | 10 | 0 | 0 | 0 | 0 |
| `playcustomapp` v1 | 1 | 1 | 0 | 0 | 0 | 0 |
| **Total** | **181** | **128** | **31** | **11** | **11** | **0** |

Of the 170 admin methods (181 total minus the 11 excluded by nature), **128 are called**, **31 are redundant** with a called method, **11 are parked** behind an issue and **0 are uncovered**.

## `androidpublisher` v3 (145 methods)

| Method | State | Commands / reason |
|---|---|---|
| `androidpublisher.applications.dataSafety` | ✅ | `gplay compliance datasafety set` |
| `androidpublisher.applications.deviceTierConfigs.create` | ✅ | `gplay device-tiers create` |
| `androidpublisher.applications.deviceTierConfigs.get` | ✅ | `gplay device-tiers view` |
| `androidpublisher.applications.deviceTierConfigs.list` | ✅ | `gplay device-tiers list` |
| `androidpublisher.applications.tracks.releases.list` | ⚪ | redundant with `androidpublisher.edits.tracks.list`: releases are read per track inside an Edit by `tracks list`; this is the Edit-less projection of the same data |
| `androidpublisher.apprecovery.addTargeting` | ✅ | `gplay recovery add-targeting` |
| `androidpublisher.apprecovery.cancel` | ✅ | `gplay recovery cancel` |
| `androidpublisher.apprecovery.create` | ✅ | `gplay recovery create` |
| `androidpublisher.apprecovery.deploy` | ✅ | `gplay recovery deploy` |
| `androidpublisher.apprecovery.list` | ✅ | `gplay recovery list` |
| `androidpublisher.appsigning.enrollApp` | ✅ | `gplay signing enroll`; --confirm-gated (ADR-0043) |
| `androidpublisher.appsigning.rotateAppSigningKey` | ✅ | `gplay signing rotate`; --confirm-gated (ADR-0043) |
| `androidpublisher.appstoreappsreview.createappstorehostedapp` | ✅ | `gplay appstore create` |
| `androidpublisher.appstoreappsreview.updateappstorehostedapp` | ✅ | `gplay appstore update`; --confirm-gated: sends the app to review (ADR-0043) |
| `androidpublisher.appstoreappsreview.updateappstorehostedapppublishstatus` | ✅ | `gplay appstore publish-status` |
| `androidpublisher.appstoreappsreview.uploadapk` | ✅ | `gplay appstore upload apk` |
| `androidpublisher.appstoreappsreview.uploadappstoreapppolicydeclarationfile` | ✅ | `gplay appstore upload policy` |
| `androidpublisher.appstoreappsreview.uploadimage` | ✅ | `gplay appstore upload image` |
| `androidpublisher.appstorecatalog.recentappviews.get` | ✅ | `gplay appstore catalog view` |
| `androidpublisher.appstorecatalog.recentupdateevents.list` | ✅ | `gplay appstore catalog events list` |
| `androidpublisher.edits.apks.addexternallyhosted` | 🔴 | parked, [#546](https://github.com/PollyGlot/google-play-cli/issues/546): externally hosted APKs are a Managed Play only flow |
| `androidpublisher.edits.apks.list` | ✅ | `gplay releases artifacts list`; the apk kind; read inside a read-only Edit or the pinned explicit one |
| `androidpublisher.edits.apks.upload` | ✅ | `gplay releases upload`; an .apk payload rides releases upload (ADR-0036) |
| `androidpublisher.edits.bundles.list` | ✅ | `gplay releases artifacts list`; the bundle kind; read inside a read-only Edit or the pinned explicit one |
| `androidpublisher.edits.bundles.upload` | ✅ | `gplay releases upload`; the .aab path, resumable upload host |
| `androidpublisher.edits.commit` | ✅ | `gplay edits commit`; also called implicitly at the end of every mutating Edit-scoped command |
| `androidpublisher.edits.countryavailability.get` | ✅ | `gplay tracks availability view` |
| `androidpublisher.edits.delete` | ✅ | `gplay edits discard`; also called to drop the read-only Edit opened by list/view commands |
| `androidpublisher.edits.deobfuscationfiles.upload` | ✅ | `gplay releases mappings upload` |
| `androidpublisher.edits.details.get` | ✅ | `gplay apps details view`, `gplay apps view`, `gplay apps audit` |
| `androidpublisher.edits.details.patch` | ✅ | `gplay apps details set` |
| `androidpublisher.edits.details.update` | ⚪ | redundant with `androidpublisher.edits.details.patch`: full-body `update` of App details; `apps details set` writes field by field through `patch` |
| `androidpublisher.edits.expansionfiles.get` | ✅ | `gplay releases expansion-files view` |
| `androidpublisher.edits.expansionfiles.patch` | ⚪ | redundant with `androidpublisher.edits.expansionfiles.update`: partial write of a single-field resource; `releases expansion-files set` uses `update` |
| `androidpublisher.edits.expansionfiles.update` | ✅ | `gplay releases expansion-files set` |
| `androidpublisher.edits.expansionfiles.upload` | ✅ | `gplay releases expansion-files upload` |
| `androidpublisher.edits.get` | ✅ | `gplay edits status`; only with --live; the default status is a local read of the pin |
| `androidpublisher.edits.images.delete` | ✅ | `gplay metadata images apply`; --prune drops one live image by id |
| `androidpublisher.edits.images.deleteall` | ✅ | `gplay metadata images apply`; slot reconciliation clears a slot before re-upload (ADR-0013) |
| `androidpublisher.edits.images.list` | ✅ | `gplay metadata images list`, `gplay metadata images pull`, `gplay metadata images apply` |
| `androidpublisher.edits.images.upload` | ✅ | `gplay metadata images apply` |
| `androidpublisher.edits.insert` | ✅ | `gplay edits begin`; also opened implicitly by every Edit-scoped command (implicit Edit mode, docs/DESIGN.md) |
| `androidpublisher.edits.listings.delete` | ✅ | `gplay metadata apply`; --prune drops a locale that left the local tree |
| `androidpublisher.edits.listings.deleteall` | ⚪ | redundant with `androidpublisher.edits.listings.delete`: drops every locale at once; `metadata apply --prune` deletes locale by locale and refuses the defaultLanguage Listing (ADR-0011) |
| `androidpublisher.edits.listings.get` | ✅ | `gplay metadata pull` |
| `androidpublisher.edits.listings.list` | ✅ | `gplay metadata list`, `gplay metadata pull`, `gplay metadata images list` |
| `androidpublisher.edits.listings.patch` | ✅ | `gplay metadata apply`; writes the changed fields of a locale already live (ADR-0011: missing is not empty) |
| `androidpublisher.edits.listings.update` | ✅ | `gplay metadata apply`; creates a locale absent online, which patch cannot (#561) |
| `androidpublisher.edits.testers.get` | ✅ | `gplay testers list` |
| `androidpublisher.edits.testers.patch` | ⚪ | redundant with `androidpublisher.edits.testers.update`: partial write of a single-field resource; `testers set` replaces the whole group list through `update` |
| `androidpublisher.edits.testers.update` | ✅ | `gplay testers set` |
| `androidpublisher.edits.tracks.create` | ✅ | `gplay tracks create` |
| `androidpublisher.edits.tracks.get` | ✅ | `gplay tracks view`, `gplay releases list`, `gplay releases promote`, `gplay releases rollout`; read-before-write: tracks.update replaces the whole releases array |
| `androidpublisher.edits.tracks.list` | ✅ | `gplay tracks list`, `gplay apps audit` |
| `androidpublisher.edits.tracks.patch` | ⚪ | redundant with `androidpublisher.edits.tracks.update`: partial write of a track; every release command sends the full track body through `update` |
| `androidpublisher.edits.tracks.update` | ✅ | `gplay releases upload`, `gplay releases promote`, `gplay releases rollout`, `gplay releases halt`, `gplay releases resume`, `gplay releases complete`; the single write behind every release-state change (ADR-0002) |
| `androidpublisher.edits.validate` | ✅ | `gplay edits validate` |
| `androidpublisher.externaltransactions.createexternaltransaction` | 🔴 | parked, [#295](https://github.com/PollyGlot/google-play-cli/issues/295): alternative billing / DMA reporting: an external-transactions pipeline, not a catalog write |
| `androidpublisher.externaltransactions.getexternaltransaction` | 🔴 | parked, [#295](https://github.com/PollyGlot/google-play-cli/issues/295): alternative billing / DMA reporting: an external-transactions pipeline, not a catalog read |
| `androidpublisher.externaltransactions.refundexternaltransaction` | 🔴 | parked, [#295](https://github.com/PollyGlot/google-play-cli/issues/295): alternative billing / DMA reporting: money-moving on an external transaction |
| `androidpublisher.generatedapks.download` | ✅ | `gplay releases generated download`; binary body written to a file (ADR-0034) |
| `androidpublisher.generatedapks.list` | ✅ | `gplay releases generated list` |
| `androidpublisher.grants.create` | ✅ | `gplay team grants set` |
| `androidpublisher.grants.delete` | ✅ | `gplay team grants remove` |
| `androidpublisher.grants.patch` | ✅ | `gplay team grants set` |
| `androidpublisher.inappproducts.batchDelete` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.delete`: legacy write; gplay never writes the legacy surface, `iap apply` deletes through v2 (ADR-0041 §8) |
| `androidpublisher.inappproducts.batchGet` | ⚪ | redundant with `androidpublisher.inappproducts.list`: multi-id read; `iap pull` reads the whole legacy catalog through `list` (ADR-0041 §8) |
| `androidpublisher.inappproducts.batchUpdate` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.patch`: legacy write; gplay never writes the legacy surface, `iap apply` writes through v2 (ADR-0041 §8) |
| `androidpublisher.inappproducts.delete` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.delete`: legacy write; gplay never writes the legacy surface, `iap apply` deletes through v2 (ADR-0041 §8) |
| `androidpublisher.inappproducts.get` | ⚪ | redundant with `androidpublisher.inappproducts.list`: single-id read; `iap pull` reads the whole legacy catalog through `list` (ADR-0041 §8) |
| `androidpublisher.inappproducts.insert` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.patch`: legacy write; gplay never writes the legacy surface, `iap apply` creates through v2 `patch` with allowMissing (ADR-0041 §8) |
| `androidpublisher.inappproducts.list` | ✅ | `gplay iap pull`; read-only legacy union; gplay never writes the legacy surface (ADR-0041 §8) |
| `androidpublisher.inappproducts.patch` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.patch`: legacy write; gplay never writes the legacy surface, `iap apply` writes through v2 (ADR-0041 §8) |
| `androidpublisher.inappproducts.update` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.patch`: legacy write; gplay never writes the legacy surface, `iap apply` writes through v2 (ADR-0041 §8) |
| `androidpublisher.internalappsharingartifacts.uploadapk` | ✅ | `gplay releases sharing upload` |
| `androidpublisher.internalappsharingartifacts.uploadbundle` | ✅ | `gplay releases sharing upload` |
| `androidpublisher.monetization.convertRegionPrices` | ✅ | `gplay subscriptions prices convert` |
| `androidpublisher.monetization.onetimeproducts.batchDelete` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.delete`: multi-id form; `iap apply` deletes one product per call so each refusal maps to one product |
| `androidpublisher.monetization.onetimeproducts.batchGet` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.list`: multi-id read; `iap pull` reads the whole catalog through `list` |
| `androidpublisher.monetization.onetimeproducts.batchUpdate` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.patch`: multi-id form; `iap apply` upserts one product per call so each refusal maps to one product |
| `androidpublisher.monetization.onetimeproducts.delete` | ✅ | `gplay iap apply` |
| `androidpublisher.monetization.onetimeproducts.get` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.list`: single-id read; `iap pull` reads the whole catalog through `list` |
| `androidpublisher.monetization.onetimeproducts.list` | ✅ | `gplay iap pull` |
| `androidpublisher.monetization.onetimeproducts.patch` | ✅ | `gplay iap apply`; allowMissing makes patch the create: the API has no insert |
| `androidpublisher.monetization.onetimeproducts.purchaseOptions.batchDelete` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.patch`: purchase options are nested in the product body; a dropped option leaves through the product `patch` (ADR-0041) |
| `androidpublisher.monetization.onetimeproducts.purchaseOptions.batchUpdateStates` | ✅ | `gplay iap apply`; declarative `state:` on a purchase option; the only state write path of that sub-resource |
| `androidpublisher.monetization.onetimeproducts.purchaseOptions.offers.activate` | ✅ | `gplay iap apply`; declarative `state:` on a one-time offer |
| `androidpublisher.monetization.onetimeproducts.purchaseOptions.offers.batchDelete` | ✅ | `gplay iap apply` |
| `androidpublisher.monetization.onetimeproducts.purchaseOptions.offers.batchGet` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.purchaseOptions.offers.list`: multi-id read; `iap pull` reads every offer of a purchase option through `list` |
| `androidpublisher.monetization.onetimeproducts.purchaseOptions.offers.batchUpdate` | ✅ | `gplay iap apply`; the only offer write path on one-time products |
| `androidpublisher.monetization.onetimeproducts.purchaseOptions.offers.batchUpdateStates` | ⚪ | redundant with `androidpublisher.monetization.onetimeproducts.purchaseOptions.offers.activate`: batch state verb; `iap apply` drives the unary `activate`/`deactivate`/`cancel` verbs, one offer per call (ADR-0041) |
| `androidpublisher.monetization.onetimeproducts.purchaseOptions.offers.cancel` | ✅ | `gplay iap apply`; --confirm-gated: cancelling a pre-order offer is irreversible |
| `androidpublisher.monetization.onetimeproducts.purchaseOptions.offers.deactivate` | ✅ | `gplay iap apply` |
| `androidpublisher.monetization.onetimeproducts.purchaseOptions.offers.list` | ✅ | `gplay iap pull` |
| `androidpublisher.monetization.subscriptions.archive` | ⚪ | redundant with `androidpublisher.monetization.subscriptions.patch`: deprecated in Discovery ("subscription archiving is not supported"); the archived state is output-only, lifecycle rides `patch`/`delete` (ADR-0041) |
| `androidpublisher.monetization.subscriptions.basePlans.activate` | ✅ | `gplay subscriptions apply`; declarative `state:` on a base plan |
| `androidpublisher.monetization.subscriptions.basePlans.batchMigratePrices` | ⚪ | redundant with `androidpublisher.monetization.subscriptions.basePlans.migratePrices`: multi-plan form; `subscriptions prices migrate` gates one base plan per call (ADR-0041) |
| `androidpublisher.monetization.subscriptions.basePlans.batchUpdateStates` | ⚪ | redundant with `androidpublisher.monetization.subscriptions.basePlans.activate`: batch state verb; `subscriptions apply` drives the unary `activate`/`deactivate` verbs, one base plan per call (ADR-0041) |
| `androidpublisher.monetization.subscriptions.basePlans.deactivate` | ✅ | `gplay subscriptions apply` |
| `androidpublisher.monetization.subscriptions.basePlans.delete` | ✅ | `gplay subscriptions apply`; --confirm-gated; the API only deletes a DRAFT base plan |
| `androidpublisher.monetization.subscriptions.basePlans.migratePrices` | ✅ | `gplay subscriptions prices migrate`; --confirm-gated, reprices live subscribers |
| `androidpublisher.monetization.subscriptions.basePlans.offers.activate` | ✅ | `gplay subscriptions apply` |
| `androidpublisher.monetization.subscriptions.basePlans.offers.batchGet` | ⚪ | redundant with `androidpublisher.monetization.subscriptions.basePlans.offers.list`: multi-id read; `subscriptions pull` reads every offer of a base plan through `list` |
| `androidpublisher.monetization.subscriptions.basePlans.offers.batchUpdate` | ⚪ | redundant with `androidpublisher.monetization.subscriptions.basePlans.offers.patch`: multi-id form; `subscriptions apply` upserts one offer per call through `create`/`patch` |
| `androidpublisher.monetization.subscriptions.basePlans.offers.batchUpdateStates` | ⚪ | redundant with `androidpublisher.monetization.subscriptions.basePlans.offers.activate`: batch state verb; `subscriptions apply` drives the unary `activate`/`deactivate` verbs, one offer per call (ADR-0041) |
| `androidpublisher.monetization.subscriptions.basePlans.offers.create` | ✅ | `gplay subscriptions apply` |
| `androidpublisher.monetization.subscriptions.basePlans.offers.deactivate` | ✅ | `gplay subscriptions apply` |
| `androidpublisher.monetization.subscriptions.basePlans.offers.delete` | ✅ | `gplay subscriptions apply` |
| `androidpublisher.monetization.subscriptions.basePlans.offers.get` | ⚪ | redundant with `androidpublisher.monetization.subscriptions.basePlans.offers.list`: single-id read; `subscriptions pull` reads every offer of a base plan through `list` |
| `androidpublisher.monetization.subscriptions.basePlans.offers.list` | ✅ | `gplay subscriptions pull` |
| `androidpublisher.monetization.subscriptions.basePlans.offers.patch` | ✅ | `gplay subscriptions apply` |
| `androidpublisher.monetization.subscriptions.batchGet` | ⚪ | redundant with `androidpublisher.monetization.subscriptions.list`: multi-id read; `subscriptions pull` reads the whole catalog through `list` |
| `androidpublisher.monetization.subscriptions.batchUpdate` | ⚪ | redundant with `androidpublisher.monetization.subscriptions.patch`: multi-id form; `subscriptions apply` upserts one subscription per call so each refusal maps to one product |
| `androidpublisher.monetization.subscriptions.create` | ✅ | `gplay subscriptions apply` |
| `androidpublisher.monetization.subscriptions.delete` | ✅ | `gplay subscriptions apply` |
| `androidpublisher.monetization.subscriptions.get` | ⚪ | redundant with `androidpublisher.monetization.subscriptions.list`: single-id read; `subscriptions pull` reads the whole catalog through `list` |
| `androidpublisher.monetization.subscriptions.list` | ✅ | `gplay subscriptions pull` |
| `androidpublisher.monetization.subscriptions.patch` | ✅ | `gplay subscriptions apply`; also carries base-plan config and per-territory prices: the sub-resource has no create/patch |
| `androidpublisher.orders.batchget` | ✅ | `gplay orders view`; the multi-id form of orders view |
| `androidpublisher.orders.get` | ✅ | `gplay orders view` |
| `androidpublisher.orders.refund` | ✅ | `gplay orders refund`; --confirm-gated, money-moving (ADR-0031) |
| `androidpublisher.orders.reviewrefund` | 🔴 | parked, [#352](https://github.com/PollyGlot/google-play-cli/issues/352): chargeback refund review; slice-vs-park still to be grilled |
| `androidpublisher.purchases.products.acknowledge` | ⚫️ | runtime: acknowledges a purchase token minted on-device, not an admin operation |
| `androidpublisher.purchases.products.consume` | ⚫️ | runtime: consumes a purchase token minted on-device, not an admin operation |
| `androidpublisher.purchases.products.get` | ⚫️ | runtime: server-side purchase-token verification |
| `androidpublisher.purchases.productsv2.getproductpurchasev2` | ⚫️ | runtime: server-side purchase-token verification (v2 shape) |
| `androidpublisher.purchases.subscriptions.acknowledge` | ⚫️ | runtime: acknowledges a subscription purchase token minted on-device |
| `androidpublisher.purchases.subscriptions.cancel` | ⚫️ | runtime: acts on a subscription purchase token, not on a catalog resource |
| `androidpublisher.purchases.subscriptions.defer` | ⚫️ | runtime: acts on a subscription purchase token, not on a catalog resource |
| `androidpublisher.purchases.subscriptionsv2.cancel` | ⚫️ | runtime: acts on a subscription purchase token (v2 shape) |
| `androidpublisher.purchases.subscriptionsv2.defer` | ⚫️ | runtime: acts on a subscription purchase token (v2 shape) |
| `androidpublisher.purchases.subscriptionsv2.get` | ⚫️ | runtime: server-side subscription-token verification (v2 shape) |
| `androidpublisher.purchases.subscriptionsv2.revoke` | ⚫️ | runtime: acts on a subscription purchase token (v2 shape) |
| `androidpublisher.purchases.voidedpurchases.list` | 🔴 | parked, [#346](https://github.com/PollyGlot/google-play-cli/issues/346): voided-purchases feed for refund/chargeback reconciliation |
| `androidpublisher.reviews.get` | ✅ | `gplay reviews view` |
| `androidpublisher.reviews.list` | ✅ | `gplay reviews list` |
| `androidpublisher.reviews.reply` | ✅ | `gplay reviews reply` |
| `androidpublisher.systemapks.variants.create` | 🔴 | parked, [#296](https://github.com/PollyGlot/google-play-cli/issues/296): system APK variants are preload/OEM system-image material |
| `androidpublisher.systemapks.variants.download` | 🔴 | parked, [#296](https://github.com/PollyGlot/google-play-cli/issues/296): system APK variants are preload/OEM system-image material |
| `androidpublisher.systemapks.variants.get` | 🔴 | parked, [#296](https://github.com/PollyGlot/google-play-cli/issues/296): system APK variants are preload/OEM system-image material |
| `androidpublisher.systemapks.variants.list` | 🔴 | parked, [#296](https://github.com/PollyGlot/google-play-cli/issues/296): system APK variants are preload/OEM system-image material |
| `androidpublisher.users.create` | ✅ | `gplay team users add` |
| `androidpublisher.users.delete` | ✅ | `gplay team users remove` |
| `androidpublisher.users.list` | ✅ | `gplay team users list`, `gplay team users view`, `gplay team grants list` |
| `androidpublisher.users.patch` | ✅ | `gplay team users set` |

## `playdeveloperreporting` v1beta1 (25 methods)

| Method | State | Commands / reason |
|---|---|---|
| `playdeveloperreporting.anomalies.list` | ✅ | `gplay vitals anomalies` |
| `playdeveloperreporting.apps.fetchReleaseFilterOptions` | 🔴 | parked, [#348](https://github.com/PollyGlot/google-play-cli/issues/348): release-keyed vitals filters, to fold into `vitals` when release filtering lands |
| `playdeveloperreporting.apps.search` | ✅ | `gplay apps accessible list`; server-authoritative discovery, distinct from the local registry (ADR-0039) |
| `playdeveloperreporting.vitals.anonrssandswapmemoryusage.get` | ✅ | `gplay vitals query anonrssandswapmemoryusage --describe` |
| `playdeveloperreporting.vitals.anonrssandswapmemoryusage.query` | ✅ | `gplay vitals query anonrssandswapmemoryusage` |
| `playdeveloperreporting.vitals.anrrate.get` | ✅ | `gplay vitals anr --describe`, `gplay vitals query anrrate --describe` |
| `playdeveloperreporting.vitals.anrrate.query` | ✅ | `gplay vitals anr`, `gplay vitals query anrrate` |
| `playdeveloperreporting.vitals.bitmapmemoryusage.get` | ✅ | `gplay vitals query bitmapmemoryusage --describe` |
| `playdeveloperreporting.vitals.bitmapmemoryusage.query` | ✅ | `gplay vitals query bitmapmemoryusage` |
| `playdeveloperreporting.vitals.crashrate.get` | ✅ | `gplay vitals crashes --describe`, `gplay vitals query crashrate --describe` |
| `playdeveloperreporting.vitals.crashrate.query` | ✅ | `gplay vitals crashes`, `gplay vitals query crashrate` |
| `playdeveloperreporting.vitals.errors.counts.get` | ✅ | `gplay vitals errors counts --describe` |
| `playdeveloperreporting.vitals.errors.counts.query` | ✅ | `gplay vitals errors counts` |
| `playdeveloperreporting.vitals.errors.issues.search` | ✅ | `gplay vitals errors issues` |
| `playdeveloperreporting.vitals.errors.reports.search` | ✅ | `gplay vitals errors reports` |
| `playdeveloperreporting.vitals.excessivewakeuprate.get` | ✅ | `gplay vitals excessivewakeup --describe`, `gplay vitals query excessivewakeuprate --describe` |
| `playdeveloperreporting.vitals.excessivewakeuprate.query` | ✅ | `gplay vitals excessivewakeup`, `gplay vitals query excessivewakeuprate` |
| `playdeveloperreporting.vitals.lmkrate.get` | ✅ | `gplay vitals lmk --describe`, `gplay vitals query lmkrate --describe` |
| `playdeveloperreporting.vitals.lmkrate.query` | ✅ | `gplay vitals lmk`, `gplay vitals query lmkrate` |
| `playdeveloperreporting.vitals.slowrenderingrate.get` | ✅ | `gplay vitals slowrendering --describe`, `gplay vitals query slowrenderingrate --describe` |
| `playdeveloperreporting.vitals.slowrenderingrate.query` | ✅ | `gplay vitals slowrendering`, `gplay vitals query slowrenderingrate` |
| `playdeveloperreporting.vitals.slowstartrate.get` | ✅ | `gplay vitals slowstart --describe`, `gplay vitals query slowstartrate --describe` |
| `playdeveloperreporting.vitals.slowstartrate.query` | ✅ | `gplay vitals slowstart`, `gplay vitals query slowstartrate` |
| `playdeveloperreporting.vitals.stuckbackgroundwakelockrate.get` | ✅ | `gplay vitals stuckbgwakelock --describe`, `gplay vitals query stuckbackgroundwakelockrate --describe` |
| `playdeveloperreporting.vitals.stuckbackgroundwakelockrate.query` | ✅ | `gplay vitals stuckbgwakelock`, `gplay vitals query stuckbackgroundwakelockrate` |

## `gamesConfiguration` v1configuration (10 methods)

| Method | State | Commands / reason |
|---|---|---|
| `gamesConfiguration.achievementConfigurations.delete` | ✅ | `gplay games achievements delete` |
| `gamesConfiguration.achievementConfigurations.get` | ✅ | `gplay games achievements view` |
| `gamesConfiguration.achievementConfigurations.insert` | ✅ | `gplay games achievements create` |
| `gamesConfiguration.achievementConfigurations.list` | ✅ | `gplay games achievements list` |
| `gamesConfiguration.achievementConfigurations.update` | ✅ | `gplay games achievements update` |
| `gamesConfiguration.leaderboardConfigurations.delete` | ✅ | `gplay games leaderboards delete` |
| `gamesConfiguration.leaderboardConfigurations.get` | ✅ | `gplay games leaderboards view` |
| `gamesConfiguration.leaderboardConfigurations.insert` | ✅ | `gplay games leaderboards create` |
| `gamesConfiguration.leaderboardConfigurations.list` | ✅ | `gplay games leaderboards list` |
| `gamesConfiguration.leaderboardConfigurations.update` | ✅ | `gplay games leaderboards update` |

## `playcustomapp` v1 (1 methods)

| Method | State | Commands / reason |
|---|---|---|
| `playcustomapp.accounts.customApps.create` | ✅ | `gplay customapps create`; account-scoped, not package-scoped (ADR-0032) |

## Maintenance

This file is output, not input. To change a row, change its source and
regenerate:

- a command starts (or stops) calling a method: edit `internal/apiregistry/registry.go`;
- a method is runtime and will never be wrapped: edit `internal/apiregistry/exclusions.go`,
  with a one-line reason;
- a method is another shape of a resource a called method already serves, or
  its decision is deferred to a `type:parking` issue: edit
  `internal/apiregistry/dispositions.go` (ADR-0047), naming the called
  canonical or the issue number;
- Google added or removed methods: `make discovery-update`, then
  `make schema-index-update`.

Then run `make coverage-update` and commit the result. A stale or hand-edited
file fails `go test ./internal/coveragedoc/` offline.
