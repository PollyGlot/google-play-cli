## subscriptions apply: com.example.app

applied to com.example.app (11 change(s)):
  created premium
  patched basic (listings, basePlans)
  created offer premium/monthly/intro
  patched offer basic/yearly/winback (phases)
  activated base plan premium/monthly (DRAFT → ACTIVE)
  activated offer premium/monthly/intro (DRAFT → ACTIVE)
  deactivated base plan basic/yearly (ACTIVE → INACTIVE)
  deactivated offer basic/yearly/winback (ACTIVE → INACTIVE)
  deleted offer basic/yearly/stale
  deleted base plan basic/old-monthly
  deleted legacy
summary: create=1 patch=1 delete=1 basePlanDelete=1 offerCreate=1 offerPatch=1 offerDelete=1 state=4 unchanged=1
